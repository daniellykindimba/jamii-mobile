import 'package:flutter/material.dart';

class DonationsComponent extends StatelessWidget {
  const DonationsComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Donations");
  }
}